# Iris ML Project
A simple logistic regression model trained on the Iris dataset.

## Steps
1. Train model using `app.py`
2. Containerize using Docker
3. Automate build with Jenkins

## Commands
- Build Docker image: `docker build -t iris-ml-app .`
- Run container: `docker run iris-ml-app`
branch b1 update
remote change made
